# The Unicode table generator emits LF output; keep the script itself LF too.
tools/gen_cpp_unicode_tables.scm  text eol=lf

# Pre-push hook scripts must stay LF or sh (git-bash) breaks on CR.
hooks/pre-push   text eol=lf
hooks/install.sh text eol=lf
